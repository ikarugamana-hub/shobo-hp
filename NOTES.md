# 作業ノート(NOTES.md)

複数のPCで並行して更新作業を行うため、作業内容をこのファイルに記録して共有します。

## 運用ルール

- **作業を始める前に必ず `git pull` を実行**し、このファイルの最新の記録を確認する。
- **作業が終わったら、下記フォーマットで追記して push する。**
- 記載は事実ベースで簡潔に。推測や未確認事項は「未確認」と明記する。
- **公開リポジトリのため、顧客名・契約金額・原価・個人情報は記載しない。**

### 追記フォーマット

```
### YYYY-MM-DD / 作業PC名(任意)
- 変更内容:
- 影響範囲(ファイル・ページ):
- 未対応・引き継ぎ事項:
```

---

## 作業履歴

### 2026-07-30(3) / Windows(claudecode)

- **変更内容:** VPS(ikaruga1007)をUbuntu 26.04 LTSにアップデートした結果、OSが初期化され、nginx・証明書・サイトファイルが全て消失。新しいSSH秘密鍵(パネルで再発行)で接続し、以下を再構築した。
  1. nginx・certbot(python3-certbot-nginx)・ffmpegを再インストール、ufw(OpenSSH/Nginx Full許可)を再設定。
  2. サイト一式(リポジトリ全体、`.git`除く)を`/var/www/shobo-hp`に再配置、所有権・パーミッションを再設定。
  3. nginx server_block(`ikaruga.sytes.net` + IP、`error_page 404`設定込み)を再作成。
  4. Let's Encrypt証明書を`ikaruga.sytes.net`向けに再取得(HTTP→HTTPS自動リダイレクト、次回更新2026-10-28)。
- **影響範囲:** VPS側のみ(サーバー設定・再配置)。GitHubリポジトリの内容に変更なし。
- **未対応・引き継ぎ事項:**
  - VPSのOSアップデートはサイト全体の初期化を伴うことが判明した。**今後VPSのOS/イメージ更新を行う際は、事前にこの再構築手順(nginx+certbot+ufw+ファイル再配置)が必要になる前提で計画すること。**
  - `~/.ssh/ikaruga_deploy`という、用途不明な鍵ファイルが作業PC(Windows)に残っていた。GitHub Actions等の自動デプロイ連携は現時点で組まれていない(NOTES.md内に記載なし)ため、使われていない残骸と判断し放置。実際に自動デプロイを組む場合は、この鍵を流用するか要検討。
  - 2026-07-28の記録にある未対応事項(VPS上のURL類が旧URLのまま、No-IPドメインの暫定運用、手動デプロイ)は引き続き未着手。

### 2026-07-30(2) / Windows(claudecode)

- **変更内容:**
  1. 通気性検証動画(`uploads/kansen-breathability-test.mp4`)を再度差し替え。元素材 11.8MB(1920x1080/19秒/音声あり)を、既存仕様(1280x720・H.264・音声なし)に合わせて圧縮(633KB)。ポスターも新動画の代表フレームから再生成(25KB、タイトルが「通気性の比較」に変更された版)。
  2. `kansen.html` の動画タグから `loop` 属性を削除。再生完了後は先頭に戻らず最後のフレームで停止するようにした(ユーザー指示による仕様変更)。
- **影響範囲:** `uploads/kansen-breathability-test.mp4`、`uploads/kansen-breathability-test-poster.webp`、`kansen.html`(GitHub PagesとVPS両方に反映済み)。
- **未対応・引き継ぎ事項:** 2026-07-28の記録にある未対応事項(VPS上のURL類が旧URLのまま、No-IPドメインの暫定運用、手動デプロイ)は引き続き未着手。

### 2026-07-30 / Windows(claudecode)

- **変更内容:** 通気性検証動画(`uploads/kansen-breathability-test.mp4`)を再度差し替え。元素材 11.3MB(1920x1080/19秒/音声あり)を、既存仕様(1280x720・H.264・音声なし)に合わせて圧縮(633KB)。ポスターも新動画の代表フレームから再生成(25KB、内容は前回とほぼ同一の2面比較・キャプション文言のみ微修正版)。
- **影響範囲:** `uploads/kansen-breathability-test.mp4`、`uploads/kansen-breathability-test-poster.webp`(GitHub PagesとVPS両方に反映済み)。
- **未対応・引き継ぎ事項:** 2026-07-28の記録にある未対応事項(VPS上のURL類が旧URLのまま、No-IPドメインの暫定運用、手動デプロイ)は引き続き未着手。

### 2026-07-28 / Windows(claudecode)

- **変更内容:**
  1. XServer VPS(契約済みインスタンス、Ubuntu 24.04)にnginxを構築し、GitHub Pagesと並行してサイトを配置。SSH鍵認証のみ許可(パスワード認証は無効化していないため要確認)。
  2. No-IPの無料ダイナミックDNS(`ikaruga.sytes.net`)をVPSのIPに割り当て、Let's Encryptで無料SSL証明書を導入(HTTP→HTTPS自動リダイレクト設定、certbotによる自動更新タイマー有効)。
  3. 通気性検証動画(`uploads/kansen-breathability-test.mp4`)を新素材に差し替え。元素材 13.2MB(1920x1080/17秒/音声あり)を、既存仕様に合わせて1280x720・H.264・音声なしに圧縮(647KB)。ポスター(`kansen-breathability-test-poster.webp`)も新動画の代表フレーム(通気度の検証・CLASS-6三層生地 vs 高通気度生地の2面比較)から再生成(25KB)。
- **影響範囲:** `uploads/kansen-breathability-test.mp4`、`uploads/kansen-breathability-test-poster.webp`(GitHub PagesとVPS両方に反映済み)。VPS側のnginx設定・certbot導入はリポジトリ外(サーバー側の変更)。
- **未対応・引き継ぎ事項:**
  - VPS上のHTML(canonical/OGP/JSON-LD)・`sitemap.xml`・`robots.txt`・QRコードは、いずれもGitHub Pagesの旧URL(`ikarugamana-hub.github.io/shobo-hp`)のままで、VPS用ドメイン向けには未更新。2026-07-25(2)の記録にある「7ファイル・41箇所」の考慮がVPS+独自ドメインでも同様に必要。
  - `ikaruga.sytes.net`はNo-IPの無料DDNSであり、稟議書で検討中の正式`.jp`ドメインとは別物。無料アカウントの失効リスクがあるため、正式ドメイン取得後は切り替えが必要。
  - VPSへの反映は現時点で手動(tar+ssh)。GitHub Pagesへのpush後、VPS側への反映は別途手動で行う必要がある(自動デプロイ未整備)。
  - VPSパネルの初期パスワードは、契約直後にチャット上へ平文で共有されたため、変更を推奨済み(実施有無は未確認)。

### 2026-07-27 / Mac(manabutsurumaki)

**5つのサブエージェントを並列起動し、パフォーマンス/SEO/アクセシビリティ/ページ間一貫性/コード品質の
5観点でサイト全体を調査した。指摘のうち、確実に正しいと自分で検証できたものを修正した。**

#### 修正した重大なバグ:祝日データ切れガードが2ページに未適用だった

- **発見経緯:** コード品質監査のサブエージェントが、
  「index.html/sp.htmlのみ `holidayDataCoversToday()` によるガードがあり、
  kansen.html/story_cleaning.dc.html には入っていない」と指摘。
- **検証:** `grep -c "holidayDataCoversToday" *.html` で実際に0件であることを確認。
  これは2026-07-25(3)の作業で祝日データ切れ対策を追加した際、
  `?v=2` のキャッシュ対策(4ファイル共通の `business-hours.js` 側)はできていたが、
  **各ページに個別コピペされている `apply()` 関数側の呼び出しは index.html/sp.html にしか
  反映していなかった。**NOTES.md の過去記述「4ファイルに適用」はキャッシュ対策の話であり、
  ガードそのものの適用漏れには気づいていなかった。
- **修正:** kansen.html・story_cleaning.dc.html の `apply()` に `dataOK` 判定を追加。
  データ切れ時は `bizBadge`/`mcTel`/`mcForm` を断定表示せず「受付時間 平日 10:00〜17:00」の
  中立表示に切り替える(index.htmlと同じロジック)。
- **検証:** 両ページで `HOLIDAY_DATA_UNTIL` を意図的に古い年に書き換えて `apply()` を再実行し、
  通常時→データ切れ時→復元後の3状態が正しく切り替わることを実機で確認した。

#### 修正した軽微な問題

- **sitemap.xml の `lastmod` が実際の更新日より2日古かった**(2026-07-23固定、実際は07-25まで更新)。
  3件とも07-25に更新。
- **パンくずの区切り記号「›」が読み上げられていた**(index.html/kansen.html/story_cleaning.dc.html)。
  `aria-hidden="true"` を追加。
- **「詳しく見る →」が複数リンクで同一テキストになっており、スクリーンリーダーのリンク一覧では
  区別できなかった**(index.html 2箇所、sp.html 2箇所、kansen.html 3箇所、story_cleaning.dc.html 1箇所)。
  各リンクに `aria-label` でサービス名を補った(表示テキストは変更していない)。
- 上記に伴い、装飾矢印(`→`)の一部に `aria-hidden="true"` を追加。

#### 判断が必要なため見送った指摘(対応していない)

- **404.html に電話・フォーム導線がない。** 他4ページの固定バーが無い。意図的な簡略化か要確認。
- **index.html の受付バッジ(「受付中」/「受付時間外」)だけ、他3ページ(「ただいま電話受付中です」等)より
  短い表記になっている。** ヘッダーの限られたスペース向けに意図的に短くした可能性があり、
  デザイン判断が必要なため変更していない。
- **H1に具体的なサービス名を入れるか**(2026-07-25(4)から継続、方針判断が必要)。
- **`<main>` ランドマークが全ページに存在しない。** 影響は軽微だが、5ページ横断の構造変更になるため
  今回は見送り。
- **story_cleaning.dc.html に非標準タグ(`<x-dc>`, `<helmet>`)がある可能性、bodyタグ内に`<style>`がある
  可能性。**サブエージェントの報告のみで自分では未検証。要確認。
- **同じgtag送信ロジック・bizBadge更新ロジックが複数ファイルにコピペされている。**
  共通JSファイル化の余地はあるが、影響範囲が大きいため今回は見送り。
  ちょうど今回のバグ(祝日ガードの適用漏れ)は、このコピペ構造が原因の一つ。
  **今後この種の共通ロジックを変更する際は、4ファイル全てに同じ変更が入っているか
  必ず `grep` で横断確認すること。**
- **index.htmlのヒーロー動画がbackground-image方式でfetchpriority等が効かない**、
  **インラインstyle属性が大量(kansen.html 286箇所等)で保守性が低い**などのパフォーマンス/
  コード品質面の指摘は、実害が確認できておらず今回は見送り。

### 2026-07-25(4) / Mac(manabutsurumaki)

**複数AI(Claude / codex / agy)でトップページの「見やすさ」を比較評価し、指摘された問題を修正した。**

3モデルの一致点:主要テキストのサイズ・行間・コントラストは良好。弱点はスマホ版の極小文字と、
PC版サービス選択画面の情報の見つけにくさ。

#### 修正1:サービス選択画面でパネルのCTAが画面外に出る問題

- **症状:** 769px幅・650px高で「詳しく見る」ボタンが画面外(652px > 650px)に出ていた。
- **原因:** パネル行の高さが `calc(100vh - 195px)` の固定値だったため、
  幅が狭いときヘッダーが2段に折り返して 77px→123px に増えた分がそのまま下にずれていた。
- **対応:**
  - `sizePanelRow()` を追加し、ヘッダー・見出し・下部導線の**実測値**から高さを算出(resize時も再計算)。
    算術のずれを吸収するため、実測して画面外にはみ出した分を補正する処理も入れている。
  - 下限を 440px → **360px** に変更(低い画面で下限が勝ってCTAが押し出されるのを防ぐ)。
  - `@media (max-width:900px)` でパンくずを非表示にし、ヘッダーを1段(123px→**64px**)に収めた。
    パンくずは「← 戻る」ボタンと役割が重複するため。
  - `@media (max-height:720px)` で見出し部分を圧縮。
- **検証:** 769×650 / 1024×768 / 1280×800 / 1366×768 すべてでCTAと導線が画面内に収まることを確認。

#### 修正2:比較表以下(約2,500px分)が見落とされる問題

- **症状:** サービス選択画面はパネル下端が表示領域のすぐ外(815px / 領域800px)で切れ、
  比較表は962px、以降 FAQ・会社概要まで**2,495px分**が画面外。
  さらに macOS のスクロールバーはオーバーレイ式で**幅0px**のため、続きがある手がかりが皆無だった。
- **対応:**
  - パネル下に導線ボタン「2つのサービスの比較・よくあるご質問・会社概要 ↓」を追加。
    `scrollToCompare()` で比較表までスクロールし、見出しにフォーカスも移す。
  - `#screen-1` のスクロールバーを `::-webkit-scrollbar` で装飾。
    ただし **macOS ではオーバーレイ式のまま(幅0px)で常時表示にはできなかった**ため、
    実質的な手がかりは上記の導線ボタン。**この点は環境依存なので過信しないこと。**

#### 修正3:スマホ版の極小文字

- **症状:** 本文相当の**32箇所が14px未満**(10px×3、11px×8、12px×2、13px×19)。PC版は2箇所のみ。
- **対応:** 比較欄を**2列横並び→縦積み**に変更し、列幅の制約を外した(セル幅 160px→**304px**)。
  そのうえで比較欄本文 13px→**14px**(行間1.6→1.75)、サービス名ラベル 11px→13px、
  項目ラベル 13px→14px、英字サブタイトル 10px→12px、
  ヘッダーの「問い合わせ」12px→14px、下部リンク13px→14px、フッター13px→14px。
  下部のサービス別リンクも縦積みにした。
- **結果:** 14px未満は 32箇所→**14箇所**。残りは装飾的な英字ラベル(12px)と補足バッジ(13px)のみで、
  **情報テキストはすべて14px以上**。長い項目も1行に収まるようになった(セル高25px = 1行)。

#### 対応を見送った点

- **H1「命を守る現場を、確かな品質で支える」に具体的なサービス名を入れる案**(agyの提案)。
  codex は「直後に21pxで明記されているので不要」と判断し、意見が分かれた。
  **ブランドメッセージを優先するか検索流入者への即時説明を優先するかの方針判断**であり、
  技術的な優劣がないため変更していない。判断が決まったら対応すること。
- ヒーローの縦書き「SERVICE」ラベルが弱いという指摘(agy)。
  コントラストは実測 5.3:1 で基準を満たしており、同じ画面に32pxの「サービスを選ぶ」ボタンが
  あるため、現状で用は足りていると判断した。

#### 複数AI比較の運用メモ

- **gemini CLI は使えない。** 無料枠(Gemini Code Assist for individuals)のサポートが終了しており
  `IneligibleTierError` になる。Google系は **agy(Antigravity CLI)**を使う。
- **agy にはファイルパスを渡してはいけない。** headless では読み取りツールが自動拒否されるため、
  パスがあると読もうとして `no output produced` で失敗する。
  **プロンプト内に対象の全文を(行番号付きで)貼り付け、「ツールを使わない」と明示する**こと。
- agy は自己申告の確信度が高めに出る傾向があった(レンダリング未確認と自ら書きながら確信度「高」)。
  codex は同じ制約で「中」。**確信度の自己申告はそのまま信用せず、検証可能な指摘は実機で確かめること。**

### 2026-07-25(3) / Mac(manabutsurumaki)

トップページを中心にサイトの改善点を洗い出し、指摘した15件すべてを修正した。

**アクセシビリティ(実機で再現を確認して修正)**

- `.screen.hidden` が `opacity:0` のみだったため、**非表示画面内の10個のボタン・リンクがタブ順に残っていた**
  (ヒーロー画面でTab4回押すと見えない「← 戻る」にフォーカスが移る状態)。
  `visibility:hidden` の追加と `inert` 属性の付与で解消。修正後はタブ対象0個。
  → 印刷時に非表示画面が出力されなくなるのを防ぐため、`@media print` に `visibility:visible!important` を追加している。
- 画面遷移時にフォーカスが移動していなかった。`moveFocusTo()` を追加し、
  ゲート画面へは `#gateHeading`、戻る時は `#heroContent` にフォーカスを移すようにした。
- フォーカス枠が白固定だったため、白背景の区画で見えなかった(sp.html はコントラスト比1.07:1)。
  `#1f6fb0` に変更(白背景 5.3:1 / 濃紺背景 3.52:1 でどちらも基準を満たす)。暗色区画のみ白枠に切り替え。
- 比較表に `th scope="col"/"row"`(計8個)、非表示 `caption`、横スクロール領域に `tabindex="0"` と `aria-label` を追加。
- `prefers-reduced-motion` で `transition` も停止するようにした(以前は `animation` のみ)。

**UX**

- ヒーロー画面でスクロール操作が完全に無反応だった(`scrollHeight === innerHeight`)。
  ホイール・上スワイプ・PageDown/↓キーで `goToGate()` を呼ぶようにした。
- 戻るボタンでサイトを離脱していた。`pushState`/`popstate` を実装し、
  ゲート画面のURLを `#service` にした。`#service` 付きで開くとゲート画面から始まる。
- 読み込み後の画面幅変更でリダイレクトが再判定されなかった。`resize` で再判定(400msデバウンス)。
  **あわせて sp.html の自動リダイレクト先を `index.html?pc=1` → `index.html` に変更した。**
  `?pc=1` は `forcePC` を立てるため、自動リダイレクトで使うと以後スマホ幅にしてもSP版に戻れなくなっていた。
  `?pc=1` はフッターの「PC版サイトを表示」(利用者の明示的な選択)だけで使う。

**コンテンツ・表記**

- PC版トップに「初回お試し無償クリーニング・サンプル相談 実施中」のバッジを追加(SP版のみだった)。
- PC版ヘッダーに電話番号 0120-630-080 と受付状況、フォームCTAを追加(以前は会社概要まで下がらないと電話番号が出なかった)。
- PC版フッターにプライバシーポリシーへのリンクとGA利用の明示を追加(他3ページにはあった)。

**保守性**

- **祝日データ切れの安全化:** `HOLIDAY_DATA_UNTIL`(現在2030)と `holidayDataCoversToday()` を追加。
  データが今日をカバーしない場合、受付状況を断定せず「受付時間 平日10:00〜17:00」の中立表示に切り替える。
  → **HOLIDAYS を延長したら `HOLIDAY_DATA_UNTIL` も必ず更新すること。**
- `business-hours.js` の読み込みに `?v=2` を付与(4ファイル)。祝日更新時にキャッシュで古い判定が残るのを防ぐ。
- 重複していた `dataLayer`/`gtag` の定義を削除(index/sp)。
- フォント700をpreloadに追加(太字のちらつき対策)。ヒーロー動画に `preload="metadata"` を指定。
- sp.html の `setInterval(reveal, 300)` が永久に回っていたため、全要素表示後に停止するようにした。

**検証結果:** 5ページすべてでコンソールエラー0件、画像切れ0件、横スクロールなし。
ホイール遷移・履歴の戻る・フォーカス移動・双方向のPC/SP切り替えを実機で確認済み。

### 2026-07-25(2) / Mac(manabutsurumaki)

- **通気性検証動画を差し替え:** `uploads/kansen-breathability-test.mp4` を新しい2面比較版(他社/当社)に差し替え。
  元素材 11.9MB(1920x1080/17秒)を既存仕様に合わせて 1280x720・H.264・音声なしに圧縮(556KB)。
  マークアップ上 `muted` 再生のため音声トラックは削除した。
- **サムネイルも再生成:** 旧サムネイルは3面比較(CLASS 6 三層生地/通気性生地/不織布)で、
  新動画の2面比較と内容が食い違うため、新動画のフレームから `kansen-breathability-test-poster.webp` を再生成(28KB)。
- **`docs/稟議書_ドメイン取得とレンタルサーバー費用.md` を追加:** 独自ドメイン取得+レンタルサーバー運用の稟議用見積(概算)。
- **全ページの表示確認を実施:**`index.html` / `kansen.html` / `sp.html` / `story_cleaning.dc.html` / `404.html` を
  ローカルサーバーで確認。画像切れ・横スクロール・テキストの見切れ・コンソールエラーはいずれも検出されず。
  通気性動画の拡大・再生・閉じる動作も正常。
- **要対応(重要):独自ドメインへ移設する場合、旧URL(`/shobo-hp/`)の修正が7ファイル・計41箇所必要。**
  当初 `404.html` のみと記録していたが、別モデル(codex)のレビュー指摘を受けて全数を再確認した結果、
  以下のとおり対象が広かった。移設作業を行う担当者は必ず全件確認すること。

  | 種類 | ファイル(箇所数) | 修正しない場合 |
  |---|---|---|
  | ルート相対パス `/shobo-hp/...` | `404.html`(11) | フォント・favicon・ロゴが読めず、リンクも切れる(表示が壊れる) |
  | 絶対URL(canonical・OGP・JSON-LD・印刷用URL) | `index.html`(5)、`kansen.html`(9)、`sp.html`(3)、`story_cleaning.dc.html`(9) | 検索結果・SNS共有で旧URLが出続ける |
  | 絶対URL | `robots.txt`(1)、`sitemap.xml`(3) | 検索エンジンに旧URLを通知し続ける |

  **QRコードも再生成が必須(検証済み)。** `images/site-qr.png` をデコードした結果、
  旧URL `https://ikarugamana-hub.github.io/shobo-hp/` が埋め込まれていた。
  `kansen.html` と `story_cleaning.dc.html` の印刷用フッターに配置されており、
  隣のURLテキストも同じ旧URLなので、**両方の差し替えが必要**。
  デコードは macOS 標準の CoreImage(swift + CIDetector)で実施。追加インストール不要。

  移設先サーバーで `404.html` をエラーページとして返す設定も必要。

- **綴りに関する調査結果:** サイト内のリンクを全数調査したところ、`unilease.co.jp` への参照が28箇所あり、
  `unielase` という綴りは1件も存在しなかった。稟議依頼時に指定された `unielase.jp` は綴り違いの可能性が高い。
  ドメイン取得前に必ず確認すること。

### 2026-07-25(1) / Mac(manabutsurumaki)

- **変更内容:** ローカルリポジトリを `origin/main` の最新(`fa1c713`)まで fast-forward で更新(114コミット分の差分を取り込み)。あわせて本ファイル `NOTES.md` を新規作成。
- **影響範囲:** ローカル環境のみ(サイトの表示内容に対する変更なし)。`NOTES.md` の追加のみがリポジトリへの変更。
- **確認したこと:** ローカルサーバー(`python3 -m http.server`)でトップページの表示を確認。ヒーロー動画・ロゴ・「初回お試し無償クリーニング・サンプル相談 実施中」バナー・電話/フォーム固定バーが正常に表示された。
- **削除したファイル:** 未追跡のまま残っていた `消防HP案8_ナビゲーション.html`(旧ファイルの残骸)。pull 時点で既に消えていたため、追加の削除操作は不要だった。
- **push 時に判明した追加更新:** 本ファイルを push しようとした時点で、他PCから 7 コミット(`fa1c713` → `3dcad57`)が先に push されていた。競合はなかったため rebase して取り込み済み。内容は以下のとおり。
  - 通気性テスト動画のUI改善(サムネイル表示 → クリックで拡大、閉じるボタン追加、100cc/cm2s スペックカード下へ移動、デスクトップで幅80%に縮小)
  - サイト全体の調整(テキストの見切れ修正、アクセシビリティ、パフォーマンス、表記統一)
  - 休日データを 2030 年まで延長、フッターの著作権表記を日本語形式に統一
- **未対応・引き継ぎ事項:**
  - 今回取り込んだ差分の内容(下記「主な更新内容」および上記の追加更新)は git の差分から読み取ったものであり、各ページの表示確認はトップページのみ。`kansen.html` / `sp.html` / `story_cleaning.dc.html` / `404.html` は未確認。
  - この履歴は 2026-07-25 以降に上記フォーマットで追記していく運用とする。

#### 取り込んだ主な更新内容(`c419c0c` → `fa1c713` の差分より)

- 新規ページの追加:`kansen.html`、`sp.html`、`404.html`
- 旧ファイルの削除:`support.js`、`HP8_nav.dc.html`、`story_infection.dc.html`、`story_rental.dc.html`、`story_rental_template.txt`、`UNILEASE 感染防止衣 LP (standalone).html`
- SEO 対応:`robots.txt`、`sitemap.xml`、OGP 画像(`og-top.jpg` / `og-kansen.jpg` / `og-cleaning.jpg`)を追加
- パフォーマンス改善:画像の webp 化、`douga0.mp4` を約 19.6MB → 約 2.9MB に圧縮、Noto Sans JP のサブセットフォントを同梱(`fonts/`)
- アイコン・ブランド周りの更新:`favicon.png`、`apple-touch-icon.png`、`logo.webp`
- スクリプト:`js/business-hours.js` を追加(営業時間判定)
- 比較用アセットの追加:`assets/*_before.webp` / `*_after.webp`、`assets/service-guide-fire.pdf`
- 動画の追加:`uploads/cleaning-panel.mp4`、`uploads/infection-panel.mp4`、`uploads/kansen-breathability-test.mp4`
